/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['black-cheap-koala-709.mypinata.cloud'], // Add the domains you want to allow here
    },
};

export default nextConfig;
